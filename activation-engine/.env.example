# Sandbox DB — Prisma read-write (new Neon project)
SANDBOX_DATABASE_URL="postgresql://..."

# Syrena DB — read-only (@neondatabase/serverless HTTP queries)
SYRENA_DATABASE_URL="postgresql://..."

RESEND_API_KEY=re_...
NEXT_PUBLIC_APP_URL=http://localhost:3000
CONTROL_TOWER_PASSWORD=changeme
